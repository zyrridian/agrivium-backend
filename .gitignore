# Node modules
node_modules/

# Environment files
.env
.env.local
.env.development.local
.env.production.local
.env.test.local

# Build and output files
dist/
out/
.next/

# TypeScript cache
*.tsbuildinfo

# Ignore Drizzle migration metadata
db/migrations/meta/_journal.json
db/migrations/meta/0000_snapshot.json
db/migrations/meta/*.sql  # Ignore failed or test migration files

# Optional: Ignore all SQL migrations if you don’t want to track them
db/migrations/*.sql

# Keep schema definitions
!db/schemas/*.ts  # Keep TypeScript schema files


# Logs & Debugging
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
